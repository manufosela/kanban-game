{
  "name": "kanban-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "El juego de los tableros Kanban — Astro + Firebase (vanilla JS, Lit)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "astro build && firebase deploy",
    "deploy:hosting": "astro build && firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only database"
  },
  "dependencies": {
    "astro": "^5.7.0",
    "firebase": "^11.6.0",
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
